import { combineReducers } from 'redux';
import { Reducers } from './Application';
import CategorySlice from "src/store/CategorySlice";

export default combineReducers({
    Reducers,
    CategorySlice
});